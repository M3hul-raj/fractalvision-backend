# FractalVision Lab Backend — Docker Compose
# Docker Compose for local testing. Production uses Google Cloud Run.

version: "3.8"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
