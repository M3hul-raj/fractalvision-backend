# FractalVision Lab Backend — Docker Compose
# Used for Railway/Render deployment only. Not used for local development.

version: "3.8"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
